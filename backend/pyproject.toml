[project]
name = "jangkau-api"
version = "0.1.0"
description = "Jangkau recommendation kitchen: filter, rank, and fill the sentence cards."
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
