Notebooks are scratch paper.

Name them `01-short-name.ipynb`, `02-short-name.ipynb` in the order you run them.
When a notebook is done, copy the stable steps into a `.py` script in the matching stage folder.
Do not import a notebook from `backend/` or `frontend/`.
