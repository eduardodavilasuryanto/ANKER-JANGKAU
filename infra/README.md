# infra

Public hosting notes for the shop window and the kitchen.

Competition rule: the site must be a public HTTPS link. Named hosts are Vercel or Netlify. MAPID MAPS is the required basemap.

Do not put secrets in this folder. Keys live in host environment variables, never in git.
