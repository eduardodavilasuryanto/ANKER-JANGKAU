# data/outputs (packed lunch)

The scored GeoJSON the shop and the kitchen both eat:

`jangkau_bogor_line_scored.geojson`

Required properties per polygon are listed in `docs/GUIDEBOOK.md` section 10.
Git ignores bulky copies. A small public copy may go to `frontend/public/data/`.
