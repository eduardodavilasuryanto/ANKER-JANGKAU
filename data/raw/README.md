# data/raw (sealed lunchboxes)

Put original MAPID dumps here and never open them to "fix" a value.

If a jar is wrong, keep it. Write a cleaned copy in `data/interim/`.
Git ignores the files. Keep only this note and `.gitkeep` on GitHub.
